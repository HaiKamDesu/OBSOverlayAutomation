<Window x:Class="TournamentAutomation.Ui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tournament Dashboard" Height="508" Width="953" MinHeight="500" MinWidth="1050" Background="#101315" Foreground="#EAEAEA">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
        <Style TargetType="Button">
            <Setter Property="Margin" Value="4" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Background" Value="#1F2933" />
            <Setter Property="Foreground" Value="#EAEAEA" />
            <Setter Property="BorderBrush" Value="#334155" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#273442" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#475569" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#1B2530" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#334155" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.6" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="4" />
            <Setter Property="Padding" Value="6" />
            <Setter Property="Background" Value="#0B0F12" />
            <Setter Property="Foreground" Value="#EAEAEA" />
            <Setter Property="BorderBrush" Value="#334155" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="4" />
            <Setter Property="Padding" Value="6" />
            <Setter Property="Background" Value="#0B0F12" />
            <Setter Property="Foreground" Value="#EAEAEA" />
            <Setter Property="BorderBrush" Value="#334155" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Focusable="False"
                                          ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="ToggleBorder"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="3"
                                                SnapsToDevicePixels="True">
                                            <Grid>
                                                <ContentPresenter Margin="0,0,26,0"
                                                                  HorizontalAlignment="Stretch"
                                                                  VerticalAlignment="Stretch" />
                                                <Path HorizontalAlignment="Right"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,10,0"
                                                      Fill="#9CA3AF"
                                                      Data="M 0 0 L 4 4 L 8 0 Z" />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ToggleBorder" Property="Background" Value="#131A20" />
                                                <Setter TargetName="ToggleBorder" Property="BorderBrush" Value="#475569" />
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="ToggleBorder" Property="Background" Value="#10171D" />
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter TargetName="ToggleBorder" Property="Opacity" Value="0.6" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                                <Grid>
                                    <ContentPresenter x:Name="ContentSite"
                                                      Margin="{TemplateBinding Padding}"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding ItemTemplate}"
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left" />
                                    <TextBox x:Name="PART_EditableTextBox"
                                             Margin="{TemplateBinding Padding}"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Foreground="{TemplateBinding Foreground}"
                                             VerticalContentAlignment="Center"
                                             HorizontalContentAlignment="Left"
                                             Visibility="Hidden"
                                             IsReadOnly="{TemplateBinding IsReadOnly}" />
                                </Grid>
                            </ToggleButton>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                    <ScrollViewer CanContentScroll="True">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="Popup" Property="MinHeight" Value="95" />
                            </Trigger>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden" />
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#6B7280" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="6" />
            <Setter Property="Background" Value="#0B0F12" />
            <Setter Property="Foreground" Value="#EAEAEA" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="#111827" />
                    <Setter Property="Foreground" Value="#EAEAEA" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#1F2933" />
                    <Setter Property="Foreground" Value="#EAEAEA" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Margin" Value="4,2,4,0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#9CA3AF" />
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#0B0F12" />
            <Setter Property="Foreground" Value="#EAEAEA" />
            <Setter Property="BorderBrush" Value="#334155" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Background" Value="#0B0F12" />
            <Setter Property="Foreground" Value="#EAEAEA" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#111827" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#1F2933" />
                    <Setter Property="Foreground" Value="#F9FAFB" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <DataTemplate x:Key="CountryItemTemplate">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Image Source="{Binding FlagPath}" Width="22" Height="16" Margin="0,0,8,0" Stretch="UniformToFill" />
                <TextBlock Text="{Binding Acronym}" VerticalAlignment="Center" Foreground="#EAEAEA" />
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="QueueMatchItemTemplate">
            <Grid Margin="0,1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="26" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="▶"
                               FontWeight="Bold"
                               Foreground="#22C55E"
                               FontSize="11"
                               Visibility="{Binding IsSelected, Converter={StaticResource BoolToVisibility}}" />
                    <TextBlock Text="◆"
                               FontWeight="Bold"
                               Foreground="#38BDF8"
                               FontSize="10"
                               Margin="0,1,0,0"
                               Visibility="{Binding IsLoaded, Converter={StaticResource BoolToVisibility}}" />
                </StackPanel>
                <Border Grid.Column="1" BorderThickness="1" BorderBrush="#253140" CornerRadius="3" Background="#0D1318" Padding="6,4">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal">
                            <TextBlock Text="{Binding MetaText}" Foreground="#94A3B8" FontSize="11" />
                            <TextBlock Text=" - " Foreground="#94A3B8" FontSize="11" />
                            <TextBlock Text="{Binding ChallongeStatusText}"
                                       Foreground="{Binding ChallongeStatusBrush}"
                                       FontSize="11"
                                       FontWeight="SemiBold" />
                        </StackPanel>
                        <TextBlock Grid.Row="1"
                                   Margin="0,2,0,0"
                                   Text="{Binding MainText}"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="#F3F4F6" />
                    </Grid>
                </Border>
            </Grid>
        </DataTemplate>

        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#0B0F12" />
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="#EAEAEA" />
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="#334155" BorderThickness="1" CornerRadius="4" Background="#0D1115" Padding="8" Margin="0,0,6,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="OBS Status:" FontWeight="SemiBold" Foreground="#9CA3AF" VerticalAlignment="Center" />
                    <TextBlock Name="ObsStatusValue" Text="Disconnected" Margin="8,0,0,0" FontWeight="Bold" Foreground="#EF4444" VerticalAlignment="Center" />
                    <Button Name="ObsCredsButton" Content="Key" Width="34" Height="24" Padding="0" Margin="6,0,18,0" Click="ObsCredsButton_Click" ToolTip="OBS credentials" />

                    <TextBlock Text="Challonge Status:" FontWeight="SemiBold" Foreground="#9CA3AF" VerticalAlignment="Center" />
                    <TextBlock Name="ChallongeStatusValue" Text="No Credentials" Margin="8,0,0,0" FontWeight="Bold" Foreground="#FACC15" VerticalAlignment="Center" />
                    <Button Name="ChallongeCredsButton" Content="Key" Width="34" Height="24" Padding="0" Margin="6,0,0,0" Click="ChallongeCredsButton_Click" ToolTip="Challonge credentials" />
                </StackPanel>
            </Border>

            <Button Name="SettingsCogButton" Grid.Column="1" Content="⚙" Width="32" Height="32" Padding="0" Margin="0" Click="SettingsCogButton_Click" ToolTip="Settings">
                <Button.ContextMenu>
                    <ContextMenu Name="SettingsMenu">
                        <MenuItem Header="OBS Credentials" Click="ObsCredsButton_Click" />
                        <MenuItem Header="OBS Layout Mapping" Click="ObsMappingsButton_Click" />
                        <MenuItem Header="Challonge Credentials" Click="ChallongeCredsButton_Click" />
                        <MenuItem Name="MoveNextOnCommitMenuItem"
                                  Header="Move to next open match on commit to Challonge"
                                  IsCheckable="True"
                                  Click="MoveNextOnCommitMenuItem_Click" />
                        <Separator />
                        <MenuItem Header="Round Naming Rules" Click="RoundNamingRulesButton_Click" />
                        <MenuItem Header="Manage Countries" Click="ManageCountriesButton_Click" />
                        <MenuItem Header="Set settings folder" Click="SettingsButton_Click" />
                        <MenuItem Header="Manage Player Database" Click="OpenPlayerDatabaseButton_Click" />
                    </ContextMenu>
                </Button.ContextMenu>
            </Button>
        </Grid>

        <WrapPanel Name="SceneButtonsPanel" Grid.Row="1" Margin="0,8,0,6" />

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.5*" />
                <ColumnDefinition Width="1.5*" MinWidth="300" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <GroupBox Header="Player 1" Margin="0,0,6,4" Padding="4">
                    <Grid MinHeight="74">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="110" />
                            <ColumnDefinition MinWidth="120" />
                            <ColumnDefinition Width="128" />
                            <ColumnDefinition Width="103" />
                            <ColumnDefinition Width="54.25" />
                            <ColumnDefinition Width="58" />
                        </Grid.ColumnDefinitions>

                        <TextBox Name="P1Team" Grid.Column="0" ToolTip="P1 Team" VerticalAlignment="Center" Height="30" Margin="4,0,4,0" />
                        <TextBox Name="P1Name" Grid.Column="1" ToolTip="P1 Name" VerticalAlignment="Center" Height="30" Margin="4,0,4,0" />
                        <ComboBox Name="P1Country" Grid.Column="2" MinWidth="120" ItemTemplate="{StaticResource CountryItemTemplate}" ToolTip="P1 Country" VerticalAlignment="Center" Height="42" Margin="4,0,4,0" />
                        <Button Name="P1CharactersButton" Grid.Column="3" Content="Characters (X)" MinWidth="82" Click="P1CharactersButton_Click" VerticalAlignment="Center" Height="30" Margin="4,0,4,0" />

                        <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center" Height="38">
                            <TextBlock Name="P1ScoreValue" Text="0" Width="25" TextAlignment="Center" VerticalAlignment="Center" FontSize="15" FontWeight="Bold" Foreground="#E5E7EB" />
                            <StackPanel Orientation="Vertical" Margin="4,0,0,0">
                                <Button Content="▲" Width="24" Height="18" Padding="0" Margin="0,0,0,2" Click="P1Up_Click" />
                                <Button Content="▼" Width="24" Height="18" Padding="0" Margin="0" Click="P1Down_Click" />
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Grid.Column="5" Orientation="Vertical" VerticalAlignment="Center" Margin="3,0,0,0" Height="56">
                            <Button Content="Save" Width="54" Padding="8,4" Margin="0,0,0,4" Click="SaveP1_Click" />
                            <Button Content="Set" Width="54" Padding="8,4" Margin="0" Click="SetP1_Click" />
                        </StackPanel>
                    </Grid>
                </GroupBox>

                <GroupBox Header="Player 2" Margin="0,0,6,4" Padding="4">
                    <Grid MinHeight="74">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="110" />
                            <ColumnDefinition MinWidth="120" />
                            <ColumnDefinition Width="128" />
                            <ColumnDefinition Width="103" />
                            <ColumnDefinition Width="54.25" />
                            <ColumnDefinition Width="58" />
                        </Grid.ColumnDefinitions>

                        <TextBox Name="P2Team" Grid.Column="0" ToolTip="P2 Team" VerticalAlignment="Center" Height="30" Margin="4,0,4,0" />
                        <TextBox Name="P2Name" Grid.Column="1" ToolTip="P2 Name" VerticalAlignment="Center" Height="30" Margin="4,0,4,0" />
                        <ComboBox Name="P2Country" Grid.Column="2" MinWidth="120" ItemTemplate="{StaticResource CountryItemTemplate}" ToolTip="P2 Country" VerticalAlignment="Center" Height="42" Margin="4,0,4,0" />
                        <Button Name="P2CharactersButton" Grid.Column="3" Content="Characters (X)" MinWidth="82" Click="P2CharactersButton_Click" VerticalAlignment="Center" Height="30" Margin="4,0,4,0" />

                        <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center" Height="38">
                            <TextBlock Name="P2ScoreValue" Text="0" Width="25" TextAlignment="Center" VerticalAlignment="Center" FontSize="15" FontWeight="Bold" Foreground="#E5E7EB" />
                            <StackPanel Orientation="Vertical" Margin="4,0,0,0">
                                <Button Content="▲" Width="24" Height="18" Padding="0" Margin="0,0,0,2" Click="P2Up_Click" />
                                <Button Content="▼" Width="24" Height="18" Padding="0" Margin="0" Click="P2Down_Click" />
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Grid.Column="5" Orientation="Vertical" VerticalAlignment="Center" Margin="3,0,0,0" Height="56">
                            <Button Content="Save" Width="54" Padding="8,4" Margin="0,0,0,4" Click="SaveP2_Click" />
                            <Button Content="Set" Width="54" Padding="8,4" Margin="0" Click="SetP2_Click" />
                        </StackPanel>
                    </Grid>
                </GroupBox>

                <GroupBox Header="Match" Margin="0,0,6,4" Padding="4">
                    <Grid>
                        <WrapPanel>
                            <Button Name="RefreshButton" Content="Apply to OBS" Click="RefreshButton_Click" />
                            <Button Content="Swap Player Sides" Click="Swap_Click" />
                            <Button Content="Reset Match" Click="Reset_Click" />
                            <Button Content="Commit to Challonge" Click="CommitToChallonge_Click" />
                        </WrapPanel>
                    </Grid>
                </GroupBox>

                <WrapPanel Margin="0,0,6,0">
                    <Button Content="Undo" Click="Undo_Click" />
                    <Button Content="Redo" Click="Redo_Click" />
                </WrapPanel>
            </StackPanel>

            <GroupBox Header="Challonge Queue" Grid.Column="1" Margin="6,0,0,4" Padding="4">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top">
                        <DockPanel Margin="0,0,0,2">
                            <Button DockPanel.Dock="Right" Content="Refresh Queue" Click="RefreshQueueButton_Click" />
                            <Label Name="QueuePositionLabel"
                                   Content="Queue position: not loaded"
                                   Foreground="#D1D5DB"
                                   FontWeight="Normal"
                                   VerticalAlignment="Center"
                                   Margin="4,2,4,0" />
                        </DockPanel>
                    </StackPanel>
                    <ListBox Name="QueueListBox"
                             Margin="4"
                             MouseDoubleClick="QueueListBox_MouseDoubleClick"
                             PreviewMouseWheel="QueueListBox_PreviewMouseWheel"
                             PreviewMouseRightButtonDown="QueueListBox_PreviewMouseRightButtonDown"
                             SelectionChanged="QueueListBox_SelectionChanged"
                             ContextMenuOpening="QueueListBox_ContextMenuOpening"
                             ScrollViewer.CanContentScroll="True"
                             VirtualizingPanel.ScrollUnit="Item"
                             ItemTemplate="{StaticResource QueueMatchItemTemplate}"
                             />
                </DockPanel>
            </GroupBox>
        </Grid>

        <Grid Visibility="Collapsed">
            <TextBox Name="P1Characters" />
            <TextBox Name="P2Characters" />
            <TextBox Name="ChallongeTournamentBox" />
            <TextBox Name="ChallongeApiKeyBox" />
        </Grid>

        <Border Name="ActionToast"
                Grid.RowSpan="3"
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"
                Margin="0,0,0,4"
                Padding="10,6"
                CornerRadius="4"
                Background="#166534"
                BorderBrush="#1F2937"
                BorderThickness="1"
                Visibility="Collapsed">
            <TextBlock Name="ActionToastText"
                       Foreground="#F9FAFB"
                       FontWeight="SemiBold"
                       TextWrapping="Wrap"
                       MaxWidth="420" />
        </Border>
    </Grid>
</Window>
