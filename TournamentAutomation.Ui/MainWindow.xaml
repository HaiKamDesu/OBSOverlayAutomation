<Window x:Class="TournamentAutomation.Ui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tournament Dashboard" Height="745" Width="1100" Background="#101315" Foreground="#EAEAEA">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="6" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Background" Value="#1F2933" />
            <Setter Property="Foreground" Value="#EAEAEA" />
            <Setter Property="BorderBrush" Value="#334155" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#273442" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#475569" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#1B2530" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#334155" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.6" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="6" />
            <Setter Property="Padding" Value="6" />
            <Setter Property="Background" Value="#0B0F12" />
            <Setter Property="Foreground" Value="#EAEAEA" />
            <Setter Property="BorderBrush" Value="#334155" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="6" />
            <Setter Property="Padding" Value="6" />
            <Setter Property="Background" Value="#0B0F12" />
            <Setter Property="Foreground" Value="#EAEAEA" />
            <Setter Property="BorderBrush" Value="#334155" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Focusable="False"
                                          ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="ToggleBorder"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="3"
                                                SnapsToDevicePixels="True">
                                            <Grid>
                                                <ContentPresenter Margin="0,0,26,0"
                                                                  HorizontalAlignment="Stretch"
                                                                  VerticalAlignment="Stretch" />
                                                <Path HorizontalAlignment="Right"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,10,0"
                                                      Fill="#9CA3AF"
                                                      Data="M 0 0 L 4 4 L 8 0 Z" />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ToggleBorder" Property="Background" Value="#131A20" />
                                                <Setter TargetName="ToggleBorder" Property="BorderBrush" Value="#475569" />
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="ToggleBorder" Property="Background" Value="#10171D" />
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter TargetName="ToggleBorder" Property="Opacity" Value="0.6" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                                <Grid>
                                    <ContentPresenter x:Name="ContentSite"
                                                      Margin="{TemplateBinding Padding}"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding ItemTemplate}"
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left" />
                                    <TextBox x:Name="PART_EditableTextBox"
                                             Margin="{TemplateBinding Padding}"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Foreground="{TemplateBinding Foreground}"
                                             VerticalContentAlignment="Center"
                                             HorizontalContentAlignment="Left"
                                             Visibility="Hidden"
                                             IsReadOnly="{TemplateBinding IsReadOnly}" />
                                </Grid>
                            </ToggleButton>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                    <ScrollViewer CanContentScroll="True">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="Popup" Property="MinHeight" Value="95" />
                            </Trigger>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden" />
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#6B7280" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#475569" />
                    <Setter Property="Background" Value="#10171D" />
                </Trigger>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="#64748B" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="6" />
            <Setter Property="Background" Value="#0B0F12" />
            <Setter Property="Foreground" Value="#EAEAEA" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="#111827" />
                    <Setter Property="Foreground" Value="#EAEAEA" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#1F2933" />
                    <Setter Property="Foreground" Value="#EAEAEA" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Margin" Value="6,6,6,0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#9CA3AF" />
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#0B0F12" />
            <Setter Property="Foreground" Value="#EAEAEA" />
            <Setter Property="BorderBrush" Value="#334155" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Background" Value="#0B0F12" />
            <Setter Property="Foreground" Value="#EAEAEA" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#111827" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#1F2933" />
                    <Setter Property="Foreground" Value="#F9FAFB" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <DataTemplate x:Key="CountryItemTemplate">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Image Source="{Binding FlagPath}" Width="22" Height="16" Margin="0,0,8,0" Stretch="UniformToFill" />
                <TextBlock Text="{Binding Id}" VerticalAlignment="Center" Foreground="#EAEAEA" />
            </StackPanel>
        </DataTemplate>

        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#0B0F12" />
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="#EAEAEA" />
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <WrapPanel Orientation="Horizontal" Grid.Row="0">
            <Button Name="ConnectButton" Content="Connect OBS" Click="ConnectButton_Click" />
            <Button Name="RefreshButton" Content="Refresh Overlay" Click="RefreshButton_Click" />
            <Button Name="SettingsButton" Content="Settings" Click="SettingsButton_Click" />
            <Label Name="StatusLabel" Content="Disconnected" VerticalAlignment="Center" />
        </WrapPanel>

        <ScrollViewer Grid.Row="1" Margin="0,16,0,0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" CanContentScroll="False">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" MinWidth="260" />
                    <ColumnDefinition Width="2*" MinWidth="260" />
                    <ColumnDefinition Width="*" MinWidth="180" />
                </Grid.ColumnDefinitions>

                <GroupBox Header="Player 1" Margin="4">
                    <StackPanel>
                    <Label Content="Name" />
                    <TextBox Name="P1Name" />
                    <Label Content="Team" />
                    <TextBox Name="P1Team" />
                        <Label Content="Country" />
                        <ComboBox Name="P1Country"
                                  ItemTemplate="{StaticResource CountryItemTemplate}" />
                        <Label Content="Characters" />
                        <TextBox Name="P1Characters" />
                        <Button Content="Apply P1 to OBS" Click="ApplyP1_Click" />
                        <Button Content="Save P1" Click="SaveP1_Click" />
                        <Button Content="Set Player" Click="SetP1_Click" />
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Player 2" Margin="4" Grid.Column="1">
                    <StackPanel>
                    <Label Content="Name" />
                    <TextBox Name="P2Name" />
                    <Label Content="Team" />
                    <TextBox Name="P2Team" />
                        <Label Content="Country" />
                        <ComboBox Name="P2Country"
                                  ItemTemplate="{StaticResource CountryItemTemplate}" />
                        <Label Content="Characters" />
                        <TextBox Name="P2Characters" />
                        <Button Content="Apply P2 to OBS" Click="ApplyP2_Click" />
                        <Button Content="Save P2" Click="SaveP2_Click" />
                        <Button Content="Set Player" Click="SetP2_Click" />
                    </StackPanel>
                </GroupBox>

                <StackPanel Grid.Column="2">
                    <GroupBox Header="Scenes" Margin="4">
                        <StackPanel>
                            <Button Content="In-Match" Click="SceneInMatch_Click" />
                            <Button Content="Desk" Click="SceneDesk_Click" />
                            <Button Content="Break" Click="SceneBreak_Click" />
                            <Button Content="Results" Click="SceneResults_Click" />
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Scores" Margin="4">
                        <StackPanel>
                            <Button Content="P1 +1" Click="P1Up_Click" />
                            <Button Content="P1 -1" Click="P1Down_Click" />
                            <Button Content="P2 +1" Click="P2Up_Click" />
                            <Button Content="P2 -1" Click="P2Down_Click" />
                            <Button Content="Commit to Challonge" Click="CommitToChallonge_Click" />
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Match" Margin="4">
                        <StackPanel>
                            <Button Content="Swap P1 with P2" Click="Swap_Click" />
                            <Button Content="Reset Match" Click="Reset_Click" />
                            <Button Content="Load Previous" Click="Previous_Click" />
                            <Button Content="Load Next" Click="Next_Click" />
                            <Button Content="Undo" Click="Undo_Click" />
                            <Button Content="Redo" Click="Redo_Click" />
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Challonge Queue" Margin="4">
                        <StackPanel>
                            <Label Content="Tournament" />
                            <TextBox Name="ChallongeTournamentBox" />
                            <Label Content="API Key" />
                            <TextBox Name="ChallongeApiKeyBox" />
                            <Button Content="Refresh Queue from Challonge" Click="RefreshQueueButton_Click" />
                            <Button Content="Load Next Queue Match" Click="LoadNextQueueButton_Click" />
                            <Label Name="QueuePositionLabel"
                                   Content="Queue position: not loaded"
                                   Foreground="#D1D5DB"
                                   FontWeight="Normal" />
                            <ListBox Name="QueueListBox"
                                     Margin="6"
                                     Height="220"
                                     MouseDoubleClick="QueueListBox_MouseDoubleClick" />
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
