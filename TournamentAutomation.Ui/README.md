# TournamentAutomation.Ui

WPF dashboard that drives `TournamentAutomation` orchestration logic.

## Run
Set `TournamentAutomation.Ui` as the startup project in Visual Studio.

## Notes
- The UI uses the same `ConfigScript` as the console app.
- It calls into `AutomationHost` to execute the same commands.
