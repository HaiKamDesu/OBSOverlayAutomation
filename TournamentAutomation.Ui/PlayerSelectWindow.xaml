<Window x:Class="TournamentAutomation.Ui.PlayerSelectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Player" Height="420" Width="740"
        Background="#101315" Foreground="#EAEAEA"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,12">
            <Button Width="90" Margin="0,0,8,0" Content="Add" Click="Add_Click" />
            <Button Width="90" Margin="0,0,8,0" Content="Edit" Click="Edit_Click" />
            <Button Width="90" Content="Remove" Click="Remove_Click" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Search:" VerticalAlignment="Center" Foreground="#9CA3AF" Margin="0,0,8,0" />
            <TextBox Name="SearchBox"
                     Grid.Column="1"
                     Margin="0"
                     Padding="6"
                     VerticalContentAlignment="Center"
                     TextChanged="SearchBox_TextChanged"
                     ToolTip="Search nickname first, then aliases" />
            <Button Grid.Column="2"
                    Width="60"
                    Margin="8,0,0,0"
                    Content="Clear"
                    Click="ClearSearch_Click" />
        </Grid>

        <ListView Name="PlayersList"
                  Grid.Row="2"
                  Margin="0,0,0,12"
                  SelectionMode="Single"
                  MouseDoubleClick="PlayersList_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Nickname" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Team" DisplayMemberBinding="{Binding Team}" />
                    <GridViewColumn Header="Country" DisplayMemberBinding="{Binding Country}" />
                    <GridViewColumn Header="Characters" DisplayMemberBinding="{Binding Characters}" />
                    <GridViewColumn Header="Aliases" DisplayMemberBinding="{Binding AliasesDisplay}" />
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="90" Margin="6" Content="Cancel" Click="Cancel_Click" />
            <Button Width="90" Margin="6" Content="Select" Click="Select_Click" />
        </StackPanel>
    </Grid>
</Window>
